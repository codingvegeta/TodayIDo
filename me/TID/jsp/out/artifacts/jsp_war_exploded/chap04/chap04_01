JSP의 필수 이해 요소

-JSP 처리 과정
> JSP에 해당하는 서블릿이 존재하지 않을 경우
    JSP 페이지로부터 자바 코드를 생성한다.
    자바 코드를 컴파일해서 서블릿 클래스를 생성한다.
    서블릿에 클라이언트 요청을 전달한다.
    서블릿이 요청을 처리한 결과를 응답으로 생성한다.
    응답을 웹 브라우저에 전송한다.
> JSP에 해당하는 서블릿이 존재하는 경우
    서블릿에 클아이언트 요청을 전달한다.
    서블릿이 요청을 처리한 결과를 응답으로 생성한다.
    응답을 웹 브라우저에 전송한다.

JSP페이지를 요청할 때에는 JSP를 직접 실행하는것이 아닌 JSP를 자바 소스코드로 변환한 뒤 컴파일해서 생성한 서블릿을 실행하는것이다.
